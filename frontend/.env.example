# En desarrollo local no hace falta definir esto: Vite usa el proxy de vite.config.js hacia localhost:4000.
# En producción (Netlify), configurar esta variable con la URL pública del backend desplegado.
VITE_API_URL=https://tu-backend.onrender.com/api
